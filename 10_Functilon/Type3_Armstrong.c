/*.....WAP to find Armstrong number........*/
// with  no return with argumentvalue.
#include<stdio.h>
void Armstrong(int n)
{
	int temp,r,sum=0;
		temp=n;
	while(n>0)
	{
		r=n%10;
		sum=sum+r*r*r;
		n=n/10;
	}
	return(n);
}
	
int main()
{
	int n,temp,sum=0;
	printf("Enter your number for Armstrong number.\n");
	scanf("%d",&n);
	int Armstrongs=Armstrong(n);
	if(temp==sum)
		printf("Your number is Armstrong.%d",Armstrongs);
	else
		printf("your number is Not Armstrong number.%d",Armstrongs);
}























































































































































































































































































































































































































































































































































































































































































































































































































































































