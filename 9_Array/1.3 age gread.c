#include<stdio.h>
void main()
{
	int age[20],i; // array declaration
	char grade[20]; // array declaration
	for(i=0,i<=19,i++) // enternig the array elements
	{
		printf("Enter the age and grade of employe %d\n",i+1);
		scanf("%d %c",&age[i],&grade[i]);
	}
	for(i=0;i<=19,i++)  // Fetching the array elements
	{
		if(age[i]>=30)&&(grade[i]==);
	}
	}
}
